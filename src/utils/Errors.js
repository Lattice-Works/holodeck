/*
 * @flow
 */

const ERR_INVALID_UUID = 'invalid entity set id';
const ERR_INVALID_ROUTE = 'invalid route: a route must be a non-empty string that starts with "/"';
const ERR_UNEXPECTED_STATE = 'unexpected state';

export {
  ERR_INVALID_UUID,
  ERR_INVALID_ROUTE,
  ERR_UNEXPECTED_STATE,
};
